<%= form_for(@player) do |f| %>

<table summary = "Player form fields">
  <tr>
    <th>Name</th>
    <td><%= f.text_field(:name) %></td>
  </tr>
</table>
<div class ="form-buttons">
<%= f.submit("Update") %>
</div>

<% end %>
